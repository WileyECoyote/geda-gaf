# Convenience stub makefile to call the real Makefile.

@SET_MAKE@

OBJEXT = l@OBJEXT@

# Explicit so that it's the default rule.
all:
	cd .. && $(MAKE) utility/all

clean %.a %.$(OBJEXT):
	cd .. && $(MAKE) utility/$@

.PHONY: all clean

.SUFFIXES:
.SUFFIXES: .a .$(OBJEXT)
