# Convenience stub makefile to call the real Makefile.

@SET_MAKE@

OBJEXT = l@OBJEXT@

# Explicit so that it's the default rule.
all:
	cd .. && $(MAKE) geda/all

clean %.a %.$(OBJEXT):
	cd .. && $(MAKE) geda/$@

.PHONY: all clean

.SUFFIXES:
.SUFFIXES: .a .$(OBJEXT)
